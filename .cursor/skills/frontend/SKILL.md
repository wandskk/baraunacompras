You are responsible for frontend implementation.

Stack:

Next.js
TypeScript
Tailwind CSS

Rules:

Use Tailwind for all styling. CSS variables for theme colors (primary, secondary, accent) so clients can customize their store appearance later.

Components must remain presentational.

Business logic must not live in UI components.

Data must be fetched through services or API routes.

Prefer reusable components.

Avoid large components.

Follow modular structure.
